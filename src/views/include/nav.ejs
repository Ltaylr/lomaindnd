<header>
    <div class=" pt-6 text-center text-lg font-semibold text-teal-400">
        <h1 class="min-w-30">Lo Main Dnd!</h1>
    </div>
<nav aria-label="Global" class="flex justify-center p-4">
    
    <ul class="flex gap-12">
            <a class="min-w-12 text-center text-xs font-semibold text-green-500" href="/">Home</a>
            <a class="min-w-12 text-center text-xs font-semibold text-green-500" href="/campaigns">Campaigns</a>
            <a class="min-w-12 text-center text-xs font-semibold text-green-500" href="/characters">Characters</a>
        <% if ( typeof isAuthenticated != 'undefined' && isAuthenticated && typeof csrfToken != 'undefined') { %>
            <a class="min-w-12 text-center text-xs font-semibold text-green-500" href="/admin/home">Admin</a>
            <li class="text-sm/6 font-semibold text-white">
                <form action="/admin/logout" method="post">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                    <button class="min-w-12 text-center text-xs font-semibold text-green-500 logout" type="submit">Logout</button>
                </form>
            </li>
        <% } %>
    </ul>
</nav>
</header>